<UserControl x:Class="TeamsAccountManager.Views.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    
    <materialDesign:Card Padding="16" Width="500" MaxHeight="600">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- ヘッダー -->
            <TextBlock Grid.Row="0" 
                       Text="{DynamicResource ConfirmUpdate}" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Margin="0,0,0,16"/>
            
            <!-- コンテンツ -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- メッセージ -->
                <TextBlock Grid.Row="0" 
                           x:Name="MessageText"
                           Text="{Binding Message}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"/>
                
                <!-- ユーザーリスト -->
                <Border Grid.Row="1" 
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="1"
                        CornerRadius="4"
                        MaxHeight="300">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <DataGrid x:Name="UsersDataGrid"
                                  ItemsSource="{Binding Users}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{DynamicResource DisplayName}"
                                                    Binding="{Binding DisplayName}"
                                                    Width="150"/>
                                <DataGridTextColumn Header="{DynamicResource Email}"
                                                    Binding="{Binding Email}"
                                                    Width="200"/>
                                <DataGridTextColumn Header="{DynamicResource Department}"
                                                    Binding="{Binding Department}"
                                                    Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </Border>
            </Grid>
            
            <!-- アクションボタン -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Content="{DynamicResource Cancel}"
                        IsCancel="True"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="False"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Content="{DynamicResource Confirm}"
                        IsDefault="True"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="True"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>