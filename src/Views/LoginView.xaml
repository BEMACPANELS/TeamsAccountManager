<UserControl x:Class="TeamsAccountManager.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="#F5F5F5">
        <Border Background="White" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1"
                CornerRadius="8"
                Padding="32" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Width="400">
            <StackPanel>
                <!-- タイトル -->
                <TextBlock Text="Teams Account Manager" 
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                
                <!-- 説明 -->
                <TextBlock Text="Microsoft アカウントでログインしてください" 
                           FontSize="14"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Opacity="0.7"
                           Margin="0,0,0,24"/>
                
                <!-- ログインボタン -->
                <Button x:Name="LoginButton"
                        Background="#0078D4"
                        Foreground="White"
                        Height="48"
                        FontSize="16"
                        Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔐 " 
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="Microsoft でログイン" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- ステータスメッセージ -->
                <TextBlock x:Name="StatusTextBlock"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"
                           Visibility="Collapsed"/>
                
                <!-- プログレスバー -->
                <ProgressBar x:Name="LoginProgressBar"
                             IsIndeterminate="True"
                             Height="4"
                             Margin="0,16,0,0"
                             Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>