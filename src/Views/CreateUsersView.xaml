<UserControl x:Class="TeamsAccountManager.Views.CreateUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- ツールバー -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                Padding="16" Margin="16,16,16,8" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{DynamicResource CreateNewUser}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="AddRowButton" Content="{DynamicResource AddRow}" Click="AddRowButton_Click" 
                            Height="32" Padding="16,0" Margin="0,0,8,0"/>
                    <Button x:Name="RemoveRowButton" Content="{DynamicResource RemoveSelectedRows}" Click="RemoveRowButton_Click" 
                            Height="32" Padding="16,0" Margin="0,0,8,0" IsEnabled="False"/>
                    <Button x:Name="GeneratePasswordsButton" Content="{DynamicResource GeneratePasswords}" Click="GeneratePasswordsButton_Click" 
                            Height="32" Padding="16,0" Margin="0,0,8,0"/>
                    <Button x:Name="CreateUsersButton" Content="{DynamicResource CreateUsersExecute}" Click="CreateUsersButton_Click" 
                            Height="32" Padding="16,0" Background="#4CAF50" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- データグリッド -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                Margin="16,8,16,8" CornerRadius="4" ClipToBounds="True">
            <Grid>
                <DataGrid x:Name="NewUsersDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Extended"
                          SelectionUnit="FullRow"
                          SelectionChanged="DataGrid_SelectionChanged"
                          Margin="1,1,10,1"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          AlternatingRowBackground="#F8F8F8"
                          RowBackground="White">
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="表示名 *" Binding="{Binding DisplayName}" Width="1.5*"/>
                        <DataGridTextColumn Header="{DynamicResource GivenName}" Binding="{Binding GivenName}" Width="1*"/>
                        <DataGridTextColumn Header="{DynamicResource Surname}" Binding="{Binding Surname}" Width="1*"/>
                        <DataGridTextColumn Header="メールアドレス *" Binding="{Binding UserPrincipalName}" Width="2*"/>
                        <DataGridTextColumn Header="パスワード *" Binding="{Binding Password}" Width="1.5*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="{DynamicResource Department}" Binding="{Binding Department}" Width="1.5*"/>
                        <DataGridTextColumn Header="{DynamicResource JobTitle}" Binding="{Binding JobTitle}" Width="1.5*"/>
                        <DataGridTextColumn Header="{DynamicResource Country}" Binding="{Binding Country}" Width="1*"/>
                        <DataGridTextColumn Header="{DynamicResource UsageLocation}" Binding="{Binding UsageLocation}" Width="1*"/>
                        <DataGridCheckBoxColumn Header="{DynamicResource ForcePasswordChange}" Binding="{Binding ForceChangePassword}" Width="1*"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- ローディング表示 -->
                <Border x:Name="LoadingOverlay" 
                        Background="#80000000" 
                        Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" 
                                     Width="200" 
                                     Height="4"/>
                        <TextBlock Text="ユーザーを作成中..." 
                                   Foreground="White" 
                                   Margin="0,16,0,0"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ステータスバー -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="8,4" Margin="16,0,16,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="StatusTextBlock" Text="準備完了"/>
                <TextBlock Grid.Column="1" Text="* は必須項目です" Foreground="Gray"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>